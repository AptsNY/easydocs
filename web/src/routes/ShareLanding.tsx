// Task 17 fills this in. Public on purpose: /s/{token} is the anonymous share landing (spec §11), so it
// sits outside RequireAuth.
export default function ShareLanding() {
  return (
    <main data-testid="share-landing">
      <h2>Shared document</h2>
    </main>
  )
}
